spring:
  config:
    import: optional:file:./.env[.properties]

server:
  port: 8082

payriff:
  secret-key: ${PAYRIFF_SECRET_KEY:}
  base-url: https://api.payriff.com/api/v3

public:
  domain: https://sackclothed-marlana-nongeological.ngrok-free.dev
